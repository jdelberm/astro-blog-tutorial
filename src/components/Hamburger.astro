---

---

<style>
  .cross {
    .middle {
      animation: hide 200ms forwards;
    }

    .upper {
      animation: tiltdown 500ms forwards;
    }

    .lower {
      animation: tiltup 500ms forwards;
    }
  }
  @keyframes hide {
    from {
      opacity: 100%;
    }
    to {
      opacity: 0%;
    }
  }

  @keyframes tiltdown {
    from {
      transform: rotate(0) translate(0, 0);
    }
    to {
      transform: rotate(-45deg) translate(-0.75rem, 1rem);
    }
  }
  @keyframes tiltup {
    from {
      transform: rotate(0) translate(0, 0);
    }
    to {
      transform: rotate(45deg) translate(-0.25rem, -0.5rem);
    }
  }
</style>

<div class="hamburger cross">
  <span class="line upper"></span>
  <span class="line middle"></span>
  <span class="line lower"></span>
</div>
